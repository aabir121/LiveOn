<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    android:orientation="vertical"
    android:background="@drawable/background_main"
    app:layout_behavior="@string/appbar_scrolling_view_behavior"
    tools:context="com.example.buglab.liveon.activity.AlarmMainActivity"
    tools:showIn="@layout/app_bar_alarm_main">
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="5"
        android:layout_marginTop="60dp"
        android:id="@+id/alarmListRootLayout"
        android:background="@drawable/custom_emergency_contact_holder"
        >
        <ListView
            android:layout_margin="35dp"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:id="@+id/alarmListView"

            ></ListView>
    </LinearLayout>
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="1"
        android:orientation="horizontal"
        android:gravity="center"
        >
        <Button
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:background="@drawable/custom_contact_button_background"
            android:text="Add New"
            android:layout_margin="18dp"
            android:padding="5dp"
            android:textSize="10dp"
            android:textColor="@android:color/white"
         android:id="@+id/addNewAlarmActivityBtn"
            />
        <Button
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:background="@drawable/custom_emergency_button_background"
            android:text="Delete All"
            android:padding="5dp"
            android:id="@+id/deleteAllBtn"
            android:layout_margin="18dp"
            android:textSize="10dp"
            android:textColor="@android:color/white"
            />

    </LinearLayout>

</LinearLayout>
